# Where the Vite dev server proxies /api (must match server PORT in server/.env)
API_PROXY_TARGET=http://127.0.0.1:4000

# OAuth / SSO: redirect URIs and CLIENT_ORIGIN are configured on the API (server/.env),
# not here. This app only needs the proxy so /api/auth/oauth/* reaches the server.
